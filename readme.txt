данный "калькул€тор" был написан в рамках тестового задани€, основным требованием которого, был ввод данных через команды. написан на основе небольшего багажа знаний js :)

инструкци€: 
в командной строке можно ввести следующие команды: 
- var - объ€вл€ет переменные со значением 0; если переменна€ задаетс€ через var со значением - отобразитьс€ ошибка 
	(пример "var a", в массив будет добавлена перемена€ "a" со значением 0,00; var a = 1, отобразитьс€ ошибка)
- let a = 1 - объ€вл€ет переменную "a" со значением 1.00 (пример "let a = 1", в массив будет добавлена перемена€ a со значением 1,00)

- printvars - в окне "out" отображаютс€ все введеные ранее переменые 
- print a - в окне "out" отображаютс€ введеное ранее значение дл€ "a"

- fn sum = a + b - сложит значени€ введеных ранее переменных "a" и "b" и сохранит сумму, как значение дл€ переменной "sum" 
- fn sub = a - b - сложит значени€ введеных ранее переменных "a" и "b" и сохранит сумму, как значение дл€ переменной "sub" 
- fn  c = a * b - сложит значени€ введеных ранее переменных "a" и "b" и сохранит сумму, как значение дл€ переменной "c"
- fn d = a / b - сложит значени€ введеных ранее переменных "a" и "b" и сохранит сумму, как значение дл€ переменной "d" 
	(переменные об€ъевленные через fn сохран€ютс€ и в дальнейшем могут быть использованы (пример fn e = sum + sub)

- printfns - отображает значение ранее заданых/ функций

применение:
с помощью данного "калькул€тора" можно решать небольшие математические задачи (площади, периметры и т.п.) 


!¬ј∆Ќќ: если при вводе команд отображаетс€ ошибка, ввод команд в input стоит начинать с пробела :)
